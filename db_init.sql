CREATE TABLE `files` (
  `filename` varchar DEFAULT NULL,
  `type` varchar DEFAULT NULL,
  `completepath` varchar DEFAULT NULL,
  `metaname` varchar DEFAULT NULL,
  `metavalue` varchar DEFAULT NULL
);
